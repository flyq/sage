f(x)=x^2

print(f(x))
print(f)
print(f(3.1))

g(y)=2*y-1

print(f(pi))
# N() 返回近似值
print(N(f(pi)))
# print(f(pi).n())

# sqrt() 开方
# ln() 自然对数
# log() 对数 the default base is e.
print(log(8,2).n())
print(ln(e).n())

# it works in local, but not in vscode
# plot(f, (x,-3,3))

# what is binomial?
# https://en.wikipedia.org/wiki/Binomial_coefficient
